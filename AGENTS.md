# PANGEA Desktop 项目协作规则

- Desktop 是 Electron 宿主、Windows 解包/打包和产品运行时的源码仓库；PANGEA 插件源码仍以独立的 dsh-pangea 仓库为准。
- 修改前保留工作区已有改动，禁止用重置、覆盖或清理命令抹掉用户的修改、profile、任务、Run、报告或凭据。
- 修改后先执行与改动直接相关的测试，再按风险扩展到 typecheck、Electron build、真实框架集成和成品验证。

## 执行责任边界

- 用户只负责下发命令、确认需要明确授权的高风险动作，以及对最终程序进行实际手动验收。
- Agent 负责建立和维护隔离测试工程，准备依赖，执行单元/集成测试、类型检查、构建、插件前端产物生成、解包目录补丁同步、日志和退出码收集，以及自己启动的测试进程清理。
- Agent 不得把上述例行工程步骤交还给用户，不得只给出“请运行这条命令/请复制这些文件/请重新构建”的操作清单而停止执行。需要用户亲手点击或体验时，只交付已经准备好的可测试实例和明确的验收入口。
- Agent 可以停止自己启动的测试进程；不得未经授权停止用户正在运行的 Agent、Run、Harness 或修改用户的 APPDATA/profile、任务数据和报告。
- 开发调试优先使用独立的测试工程和未压缩 `win-unpacked` 目录；不得通过直接改写已签名 ZIP 冒充正式包。正式构建、推送、合入或发布仍需按用户授权执行。
- 如果环境确实阻塞自动执行，Agent 必须报告已尝试的命令、实际错误和最小阻塞点；只有物理交互、凭据或明确授权不可代办时，才要求用户介入。
