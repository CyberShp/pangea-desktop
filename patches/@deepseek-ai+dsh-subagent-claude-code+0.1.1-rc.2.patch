diff --git a/node_modules/@deepseek-ai/dsh-subagent-claude-code/lib/index.js b/node_modules/@deepseek-ai/dsh-subagent-claude-code/lib/index.js
index b29080d..3a7f9f2 100644
--- a/node_modules/@deepseek-ai/dsh-subagent-claude-code/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-subagent-claude-code/lib/index.js
@@ -294,6 +294,8 @@ function claudeQueryOptions(spec, controller, capture, captureDiagnostic) {
 	return {
 		abortController: controller,
 		cwd: spec.cwd,
+		...spec.model !== void 0 ? { model: spec.model } : {},
+		...spec.reasoningEffort !== void 0 ? { effort: spec.reasoningEffort } : {},
 		env: {
 			...scrubbedParentEnv(),
 			...spec.env
@@ -473,7 +475,7 @@ async function startClaudeCodeRun(request, spec) {
 		signal: request.signal,
 		onAbort
 	});
-	return subprocessRunHandle({
+	const run = subprocessRunHandle({
 		id: SessionId(randomUUID()),
 		result,
 		signal: request.signal,
@@ -489,6 +491,8 @@ async function startClaudeCodeRun(request, spec) {
 			}
 		}
 	});
+	run.processId = publishedChild.pid;
+	return run;
 }
 //#endregion
 //#region lib/types/index.js
@@ -533,6 +537,8 @@ var ClaudeCodeProvider = class {
 		}
 		return startClaudeCodeRun(request, {
 			cwd,
+			model: request.agentOptions?.model,
+			reasoningEffort: request.agentOptions?.reasoningEffort,
 			permissionMode: this.config.permissionMode,
 			env: this.config.env,
 			disposeGraceMs: this.config.disposeGraceMs,
